name: No Tag Alert

permissions:
  contents: read

on:
  push:
    branches:
      - "**"

jobs:
  alert:
    name: Alert - No Tag Pushed
    runs-on: [self-hosted, linux, x64, aws]
    env:
      DISCORD_WEBHOOK: ${{ secrets.DISCORD_STAGING_BUILDS_URL }}
    steps:
      - name: Send No Tag Alert
        if: env.DISCORD_WEBHOOK != ''
        run: |
          CONTENT=$(cat <<EOF
          ⚠️ **No Tag Present — CI/CD Will Not Run**

          Repository : ${{ github.repository }}
          Branch     : ${{ github.ref_name }}
          Commit     : ${{ github.sha }}
          Triggered By : ${{ github.actor }}

          Push a tag (vX.Y.Z or vX.Y.Z-RCn) to trigger the pipeline.
          EOF
          )
          curl -sS -F "content=${CONTENT}" "$DISCORD_WEBHOOK"